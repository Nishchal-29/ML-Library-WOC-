# ML Library
It includes machine algorithms like linear regression, polynomial regression, logistic regression, neural network, decision tree, K means clustering and K nearest neighbors.
All the codes are written from scratch without using any external libraries like Scikit-learn, Tensorflow, etc. 
